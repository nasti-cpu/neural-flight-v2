/**
 * Graph Engine — Modular signal system (0-1 normalized)
 *
 * This module is completely decoupled from UI (SvelteFlow).
 * It can be used for any signal processing, not just the node editor.
 */

export * from "./engine";
export * from "./types";
