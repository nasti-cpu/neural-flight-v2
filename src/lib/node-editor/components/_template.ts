/**
 * Module Template — Copy this file to create a new module
 *
 * 1. Create a Content component (MyContent.svelte) with { id, data } props
 * 2. Define a ModuleDef with component reference
 * 3. Register the module
 */

// import { Box } from "lucide-svelte";
// import MyContent from "./MyContent.svelte";
// import { registerModule } from "./registry";
// import type { ModuleDef } from "./types";
//
// export const MY_MODULE: ModuleDef = {
// 	type: "my-module",
// 	label: "My Module",
// 	icon: Box,
// 	variant: "process",
// 	component: MyContent,
// 	inputs: [{ id: "value", label: "In", side: "left", handleClass: "handle-input" }],
// 	outputs: [{ id: "out", label: "Out", side: "right", handleClass: "handle-output" }],
// 	defaultData: { value: 0.5 },
// };
//
// registerModule(MY_MODULE);
