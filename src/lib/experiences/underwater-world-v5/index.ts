/**
 * index.ts – Einstiegspunkt für die Underwater World V5 Experience.
 *
 * Exportiert nur das Manifest – die Experience wird über den Catalog
 * (catalog.ts) registriert und vom Loader gesteuert.
 */

export { manifest } from "./manifest";
